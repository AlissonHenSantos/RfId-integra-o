generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model AccessLog {
  id        Int      @id @default(autoincrement())
  tagId     String
  createdAt DateTime @default(now())
}
